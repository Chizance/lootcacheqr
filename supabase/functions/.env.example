# Local-only, for `supabase functions serve` testing. Copy to supabase/functions/.env
# (gitignored). In production this is set with `supabase secrets set` instead —
# see docs/CLAUDE_API_KEY.md.
ANTHROPIC_API_KEY=
